[package]
name = "planeradarctl"
version = "0.1.0"
edition = "2024"
rust-version = "1.97.1"
license = "MIT"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
semver = "1.0"
serde = { version = "1.0", features = ["derive"] }
thiserror = "2.0"
toml = "0.8"

[dev-dependencies]
tempfile = "3.20"

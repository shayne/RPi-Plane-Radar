nextest-version = "0.9.140"

[test-groups]
bootstrap-processes = { max-threads = 1 }

[[profile.default.overrides]]
filter = 'binary(=release_contract) | binary(=bootstrap) | binary(=bootstrap_pty)'
test-group = "bootstrap-processes"
